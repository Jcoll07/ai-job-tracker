import { NextRequest,NextResponse } from "next/server";
import { CV_FAMILIES,legacyBackupSchema } from "@jobtrackr/core"; import { createJob,findDuplicate } from "@/lib/jobs"; import { createCvVersion,listCvVersions } from "@/lib/cv";
export async function POST(req:NextRequest){const body=await req.json() as {jobs?:unknown;cvs?:Array<{name?:string;family?:string;summary?:string;content?:string}>};const parsed=legacyBackupSchema.safeParse(body);if(!parsed.success)return NextResponse.json({error:"Not a recognized backup file (expected { jobs: [...] })"},{status:400});let imported=0,skipped=0;for(const legacy of parsed.data.jobs){const company=legacy.company?.trim(),jobTitle=legacy.jobTitle?.trim();if(!company||!jobTitle){skipped++;continue;}if(findDuplicate(company,jobTitle)){skipped++;continue;}createJob({company,jobTitle,location:legacy.location??null,sourceUrl:legacy.sourceUrl&&legacy.sourceUrl!=="Manual Entry"?legacy.sourceUrl:null,salaryRange:legacy.salaryRange??null,jobType:legacy.jobType??null,experience:legacy.experience??null,skills:legacy.skills??null,emailDomain:legacy.emailDomain??null,description:legacy.description??null,status:legacy.status??"Applied",dateAdded:legacy.dateAdded??new Date().toISOString().slice(0,10),dateApplied:legacy.dateAdded??null},"import");imported++;}let importedCvs=0;if(Array.isArray(body.cvs)){const existing=new Set(listCvVersions().map(c=>`${c.name}::${c.family}`));for(const cv of body.cvs){if(!cv.name||!cv.content||!cv.family||!CV_FAMILIES.includes(cv.family as never))continue;const key=`${cv.name}::${cv.family}`;if(existing.has(key))continue;createCvVersion({name:cv.name,family:cv.family as typeof CV_FAMILIES[number],summary:cv.summary,content:cv.content});existing.add(key);importedCvs++;}}return NextResponse.json({imported,skipped,importedCvs});}
