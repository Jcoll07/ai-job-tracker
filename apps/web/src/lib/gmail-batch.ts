import {CATEGORY_TO_STATUS,TERMINAL_STATUSES,type JobStatus} from "@jobtrackr/core";import {classifyEmail,aiAvailable,parseJobPostings,personalizeJob} from "./ai";import {getDb,getSetting,setSetting} from "./db";import {createJob,findDuplicate,listJobs,matchJobForEmail,shouldAutoApply,updateJob} from "./jobs";import {fetchJobPosting} from "./fetch-job";
const TOKEN_URL="https://oauth2.googleapis.com/token",GMAIL_API="https://gmail.googleapis.com/gmail/v1/users/me",BATCH_SIZE=5,MAX_MESSAGES=10000;
function creds(){const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET;return clientId&&clientSecret?{clientId,clientSecret}:null}function labelName(){const label=(process.env.GMAIL_LABEL||"LinkedIn").trim();if(!label)throw new Error("GMAIL_LABEL is empty");return label}
async function accessToken(){const t=getSetting<any>("gmailTokens");if(!t)throw new Error("Gmail is not connected");if(Date.now()<t.expires_at-60000)return t.access_token;const c=creds();if(!c||!t.refresh_token)throw new Error("Gmail token expired and cannot be refreshed — reconnect Gmail");const r=await fetch(TOKEN_URL,{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({client_id:c.clientId,client_secret:c.clientSecret,refresh_token:t.refresh_token,grant_type:"refresh_token"})});if(!r.ok)throw new Error(`Token refresh failed: ${await r.text()}`);const d=await r.json() as any;const updated={...t,access_token:d.access_token,expires_at:Date.now()+d.expires_in*1000};setSetting("gmailTokens",updated);return updated.access_token}
async function gmailGet(path:string){const r=await fetch(`${GMAIL_API}${path}`,{headers:{authorization:`Bearer ${await accessToken()}`}});if(!r.ok)throw new Error(`Gmail API ${r.status}: ${await r.text()}`);return await r.json()}
function decode(data:string){return Buffer.from(data.replaceAll("-","+").replaceAll("_","/"),"base64").toString("utf8")}function body(p:any):string{if(p.mimeType==="text/plain"&&p.body?.data)return decode(p.body.data);if(p.parts)for(const x of p.parts){const v=body(x);if(v)return v}if(p.body?.data)return decode(p.body.data);return ""}function header(h:any[],n:string){return h.find(x=>x.name.toLowerCase()===n.toLowerCase())?.value??""}function urls(text:string):string[]{return[...new Set((text.match(/https?:\/\/[^\s<>"']+/g)||[]).map(u=>u.replace(/[),.;]+$/g,"")))]}function senderDomain(from:string){const at=from.lastIndexOf("@");return at>=0?from.slice(at+1).split(">")[0].trim():null}
function aboutTheJob(text:string):string|null{const normalized=text.replace(/\r/g,"");const match=normalized.match(/(?:^|\n|\r\n)\s*About the Job\s*[:\-]?\s*\n?([\s\S]*?)(?=\n\s*(?:About the company|Benefits|Qualifications|Requirements|Skills|Responsibilities|What you(?:'|’)ll do|How you(?:'|’)ll make an impact|Apply|See more|Show less)\b|$)/i);return match?.[1]?.trim()||null}
async function resolveLabelId(name:string){const d=await gmailGet("/labels");const labels=d.labels??[];const exact=labels.find((x:any)=>x.name===name)||labels.find((x:any)=>x.name.toLowerCase()===name.toLowerCase());if(exact)return exact.id;throw new Error(`Gmail label not found: ${name}`)}async function collect(labelId:string){const params=new URLSearchParams({labelIds:labelId,maxResults:"100",includeSpamTrash:"false"});const all:any[]=[];let token="";for(let page=0;page<100;page++){const p=new URLSearchParams(params);if(token)p.set("pageToken",token);const d=await gmailGet(`/messages?${p}`);all.push(...(d.messages??[]));token=d.nextPageToken??"";if(!token||all.length>=MAX_MESSAGES)break}return all.slice(0,MAX_MESSAGES)}function saveState(s:unknown){setSetting("gmailSyncState",s)}export function getGmailSyncState(){return getSetting("gmailSyncState")}
function similar(a:string,b:string){const x=a.toLowerCase().replace(/[^a-z0-9]/g,""),y=b.toLowerCase().replace(/[^a-z0-9]/g,"");return x===y||x.includes(y)||y.includes(x)}
export async function syncGmailBatch(){if(!aiAvailable())throw new Error("AI provider is not configured — email classification needs an available provider");const label=labelName(),labelId=await resolveLabelId(label),db=getDb();const profile=getSetting<any>("profile");const seenIds=new Set((db.prepare("SELECT gmailId FROM emails WHERE jobId IS NOT NULL OR category != 'other_job_related'").all() as any[]).map(x=>x.gmailId));let state=getSetting<any>("gmailSyncState");if(!state||!Array.isArray(state.queue)||state.queue.length===0){const queue=(await collect(labelId)).filter((m:any)=>!seenIds.has(m.id));state={queue,processed:0,total:queue.length,errors:[],startedAt:new Date().toISOString()};saveState(state)}const result:any={scanned:0,classified:0,linked:0,created:0,personalized:0,statusUpdates:[],errors:[],label,queryMode:"labelIds",done:false,processed:state.processed,total:state.total};const trackedCompanies=listJobs().filter(j=>!TERMINAL_STATUSES.includes(j.status as JobStatus)).map(j=>j.company);const chunk=state.queue.splice(0,BATCH_SIZE);result.scanned=chunk.length;
for(const m of chunk){try{const full=await gmailGet(`/messages/${m.id}?format=full`),headers=full.payload?.headers??[],from=header(headers,"From"),subject=header(headers,"Subject"),text=full.payload?body(full.payload):full.snippet||"",messageUrls=urls(text),configuredLabel=label.toLowerCase()==="linkedin",emailAbout=aboutTheJob(text);let postings:any[]=[],classification:any;if(configuredLabel){try{postings=await parseJobPostings(`Subject: ${subject}\nFrom: ${from}\n\n${text}\n\nCANDIDATE_URLS:\n${messageUrls.join("\n")}`)}catch{}}if(!postings.length){classification=await classifyEmail({from,subject,body:text,trackedCompanies});if(classification.category==="other_job_related")try{postings=await parseJobPostings(`Subject: ${subject}\nFrom: ${from}\n\n${text}\n\nCANDIDATE_URLS:\n${messageUrls.join("\n")}`)}catch{}}else classification={category:"other_job_related",company:postings[0].company||null,jobTitle:postings[0].jobTitle||null,confidence:1,summary:`${postings.length} vacancy/vacancies extracted from LinkedIn email`};result.classified++;let firstJobId:number|null=null;for(const parsed of postings){if(!parsed.company||!parsed.jobTitle)continue;let job=findDuplicate(parsed.company,parsed.jobTitle);if(!job){const candidateUrls=[parsed.sourceUrl,...messageUrls.filter((u:string)=>/linkedin\.com\/jobs|greenhouse|lever\.co|ashbyhq|workday|smartrecruiters|jobvite|bamboohr|workable/i.test(u))].filter(Boolean) as string[];let exact:any=null,verifiedUrl:string|null=null;for(const candidateUrl of [...new Set(candidateUrls)]){try{const verified=await fetchJobPosting(candidateUrl);if(!verified.open)continue;const pageParsed=await parseJobPostings(verified.content);exact=pageParsed.find((p:any)=>similar(p.company||"",parsed.company||"")&&similar(p.jobTitle||"",parsed.jobTitle||""));if(exact){verifiedUrl=verified.url;break}}catch{}}if(!exact||!verifiedUrl){result.errors.push(`${m.id}: skipped ${parsed.jobTitle}: no verified URL matched the advertised vacancy`);continue}Object.assign(parsed,exact,{sourceUrl:verifiedUrl});if(emailAbout)parsed.description=emailAbout;if(!parsed.jobType||parsed.jobType==="?")parsed.jobType="?";job=createJob({company:parsed.company,jobTitle:parsed.jobTitle,location:parsed.location,salaryRange:parsed.salaryRange,jobType:parsed.jobType??"?",experience:parsed.experience,skills:parsed.skills,emailDomain:parsed.emailDomain??senderDomain(from),description:parsed.description,sourceUrl:verifiedUrl,status:"Saved"},"gmail");result.created++;if(profile){try{setSetting(`jobContext:${job.id}`,await personalizeJob({job,profile}));result.personalized++}catch{}}}if(job){if(!firstJobId)firstJobId=job.id;result.linked++}}if(!firstJobId){const linked=matchJobForEmail(senderDomain(from),classification?.company==null?null:String(classification.company));if(linked){firstJobId=linked.id;result.linked++}}const suggested=(CATEGORY_TO_STATUS[classification.category as keyof typeof CATEGORY_TO_STATUS] as JobStatus|undefined)??null;if(firstJobId&&suggested&&classification.confidence>=.75){const job=listJobs().find(j=>j.id===firstJobId);if(job&&shouldAutoApply(job.status,suggested)){updateJob(firstJobId,{status:suggested},"gmail");result.statusUpdates.push({jobId:firstJobId,company:job.company,toStatus:suggested})}}if(!seenIds.has(m.id)){const applied=firstJobId!==null&&result.statusUpdates.some((x:any)=>x.jobId===firstJobId)?1:0;db.prepare(`INSERT INTO emails (gmailId,threadId,jobId,fromAddress,subject,snippet,receivedAt,category,confidence,summary,suggestedStatus,statusApplied) VALUES (?,?,?,?,?,?,?,?,?,?,?,?)`).run(m.id,m.threadId??null,firstJobId,from,subject,(full.snippet??"").slice(0,500),full.internalDate?new Date(Number(full.internalDate)).toISOString():new Date().toISOString(),classification.category,classification.confidence,classification.summary,suggested,applied)}}catch(e){const msg=`${m.id}: ${e instanceof Error?e.message:String(e)}`;result.errors.push(msg);state.errors.push(msg)}}state.processed+=chunk.length;result.processed=state.processed;result.total=state.total;result.done=state.queue.length===0;result.errors=[...state.errors,...result.errors];if(result.done){setSetting("gmailLastSyncAt",Math.floor(Date.now()/1000));setSetting("gmailLabelSyncReady",true);setSetting("gmailHistoricalBackfillReady",true);setSetting("gmailLastSyncResult",{at:new Date().toISOString(),...result});if(result.errors.length===0)setSetting("gmailLastSuccessfulSyncResult",{at:new Date().toISOString(),...result});saveState(null)}else saveState(state);return result}
